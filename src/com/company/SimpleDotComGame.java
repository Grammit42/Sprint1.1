package com.company;

import java.io.IOException;
import java.util.ArrayList;
import java.util.Scanner;
/*
public class SimpleDotComGame {
    public static void main(String[] args) throws IOException {
        int numOfGuesses=0;//Количество ходов пользователя.

        SimpleDotCom game= new SimpleDotCom();
        //Рандомный образом нахожу индекс середины сайта, после чего добавляю в карму и хвостпо индексу.
        int randomIndex =(int) (Math.random() * 5);
        ArrayList<int> loc={randomIndex,randomIndex+1,randomIndex+2};

        game.setLocationCells(loc);
        boolean isAlive=true;

while(isAlive==true) {
    System.out.println("Сделайте выстрел: ");
    Scanner in = new Scanner(System.in);
    String userGuess = in.nextLine();

    String result =game.checkYouSelf(userGuess);
    numOfGuesses++;

if(result.equals("Потопил")) {
        isAlive= false;
    System.out.println("Потопил");
    System.out.println("Вам потребовалось "+numOfGuesses+" попыток(и)");
    }
        }

    }
}
*/
